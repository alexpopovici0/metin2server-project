Please read Libs.txt first.
Only use this if you really need it.
These are extracted from the vm.

Place to /usr/lib if you have x32, or /usr/lib32 if you have x64 system.

If you need more lib, try to extract it from the vm.
I tested the serverfile in a FreeBSD 13.1 x64 with these libs.
